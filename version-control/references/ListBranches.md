# ListBranches

List local branches with optional purpose/description from branch metadata.

## Usage

```bash
bun run $VC_SCRIPTS/ManageBranches.ts list
bun run $VC_SCRIPTS/ManageBranches.ts current
```

**Done when:** branch list (and metadata, if any) is printed.
