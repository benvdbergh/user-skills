# SwitchBranch

Check out a branch and refresh last-used metadata when present.

## Usage

```bash
bun run $VC_SCRIPTS/ManageBranches.ts switch --name "feature-branch"
bun run $VC_SCRIPTS/ManageBranches.ts list
```

**Done when:** `HEAD` is on the requested branch.
